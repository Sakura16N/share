<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.share.device.mapper.CabinetTypeMapper">

    <resultMap type="com.share.device.domain.CabinetType" id="CabinetResult" autoMapping="true">

    </resultMap>

    <select id="selectCabinetTypeList" resultMap="CabinetResult">
        select id, name, total_slots, description, status, del_flag, create_by, create_time, update_by, update_time, remark
        from cabinet_type

    <where>
        <if test="name != '' and name is not null">and name like concat('%', #{name}, '%')</if>
        <if test="totalSlots != '' and totalSlots is not null">and total_slots = #{totalSlots}</if>
        <if test="description != '' and description is not null">and description = #{description}</if>
        <if test="status != '' and status is not null">and status = #{status}</if>
        and del_flag = 0
    </where>

    </select>
</mapper>

